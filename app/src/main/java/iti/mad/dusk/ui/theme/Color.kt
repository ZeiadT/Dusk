package iti.mad.dusk.ui.theme

import androidx.compose.ui.graphics.Color

// ── Brand ─────────────────────────────────────────────────────────────────────
val Orange = Color(0xFFFF7A00)
val OrangeLight = Color(0xFFFF9A3C)
val OrangeDark = Color(0xFFCC6200)
val OrangeDim = Color(0x33FF7A00)

// ── Dark palette ──────────────────────────────────────────────────────────────
val Dark900 = Color(0xFF0B1120)  // deepest background
val Dark800 = Color(0xFF111827)  // scaffold background
val Dark700 = Color(0xFF1A2640)  // card surface
val Dark600 = Color(0xFF1E2D45)  // card alt / elevated
val Dark500 = Color(0xFF243350)  // borders, dividers
val Dark400 = Color(0xFF3A4F6E)  // disabled / muted borders

// ── Light palette ─────────────────────────────────────────────────────────────
val Light100 = Color(0xFFFFFFFF)
val Light200 = Color(0xFFF5F7FA)  // scaffold background
val Light300 = Color(0xFFEBEFF5)  // card surface
val Light400 = Color(0xFFDDE3EE)  // card alt
val Light500 = Color(0xFFC4CEDF)  // borders, dividers
val Light600 = Color(0xFF9AAABB)  // muted borders

// ── Text — dark mode ──────────────────────────────────────────────────────────
val TextDarkPrimary = Color(0xFFF0F4FF)
val TextDarkSecondary = Color(0xFF8A9BB8)
val TextDarkDisabled = Color(0xFF4A5A72)

// ── Text — light mode ─────────────────────────────────────────────────────────
val TextLightPrimary = Color(0xFF0D1B2E)
val TextLightSecondary = Color(0xFF4A6080)
val TextLightDisabled = Color(0xFF9AAABB)

// ── Semantic ──────────────────────────────────────────────────────────────────
val ErrorRed = Color(0xFFE53935)
val ErrorRedDim = Color(0x22E53935)
val SuccessGreen = Color(0xFF2E7D32)
val IceBlue = Color(0xFF7EB8D4)